import 'package:flutter/material.dart';

import '../theme/dices_theme.dart';

class RowOnHeader extends StatelessWidget {
  const RowOnHeader({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(height: 50, color: MyColors.corRosa);
  }
}
